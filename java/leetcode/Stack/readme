栈的应用：
    二叉树的遍历
        先序遍历（le144.java）
            由根节点沿左子树一直遍历下去，在遍历过程中每经过一个节点就输出该节点的信息并同时将其压栈。当某个节点无左子树时就将这个节点
            由栈弹出，并从这个节点的右子树继续沿着左子树向下遍历（对此时右子树的根节点也进行输出和压栈操作），直到栈中无任何节点时。
        中序遍历（le94.java）
            和先序遍历过程基本相同，仅仅是输出节点信息的语句位置发生了变化，即每当需要沿当前节点的右子树跟开始继续沿其左子树向下遍历时（即
            已经遍历过当前的左子树了），就先输出这个节点的信息。
        后序遍历


    后缀式求值（逆波兰表达式求值）
